interface:
  display_name: "AgentPolicy"
  short_description: "Diagnose Agents, guide policies, and structure context"
  default_prompt: "Use $agent-policy to show diagnostics, policy guidance, and task-context structuring."

policy:
  allow_implicit_invocation: false
