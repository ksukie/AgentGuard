# Repository guidance

Replace the placeholders below with rules that are true for this repository. Keep this file focused on durable, project-specific guidance; do not repeat personal preferences that belong in global `AGENTS.md`.

## Project commands

- Install: `<command>`
- Build: `<command>`
- Test: `<command>`
- Lint or format check: `<command>`

## Project conventions

- Read and follow `.editorconfig`, `.gitattributes`, and existing local `AGENTS.md` files before changing text files.
- Preserve the repository's established encoding, line-ending, interpreter, and shell conventions.
- Do not modify generated files, vendored code, lockfiles, or binary assets unless the task explicitly requires it.
- Use commands that work with paths containing spaces, Chinese characters, and emoji.

## Repository-specific rules

- Runtime and package-manager versions: `<details>`
- Required verification before handoff: `<details>`
- Generated or third-party file exceptions: `<details>`
- Deployment, security, or data-handling constraints: `<details>`
