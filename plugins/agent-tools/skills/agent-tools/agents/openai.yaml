interface:
  display_name: "AgentTools"
  short_description: "Diagnose Agents and terminal startup, audit Skills, and structure context"
  default_prompt: "Use $agent-tools to diagnose a terminal startup error, show policy guidance, structure context, or list current Codex Skills."

policy:
  allow_implicit_invocation: false
